
#Preserve "white" spaces on empty lines.
default: &default
    configCache: false
    tmpdir: "{API_PROJECT_DIR}tmp/"
    
    hosts:
        phpcrbrowser.markri.dev:
    
    exceptionhandler:
        "*": default
        
    allowDomDump: true
    
    lang:
        dir: "{API_PROJECT_DIR}lang"
        fileprefix: lang
        default: en
        languages: [en]
    
    jcr: 
        default:
            url: http://localhost:8080/server
            workspace: default
            user: admin
            password: admin
